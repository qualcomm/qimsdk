cmake_minimum_required(VERSION 3.16)

file(GLOB APP_BUILDERS_CONFIG_FILES
  "${CMAKE_CURRENT_SOURCE_DIR}/*.yml"
  "${CMAKE_CURRENT_SOURCE_DIR}/*.yaml"
)

if(APP_BUILDERS_CONFIG_FILES)
  install(
    FILES ${APP_BUILDERS_CONFIG_FILES}
    DESTINATION ${CMAKE_INSTALL_SYSCONFDIR}/qimsdk
  )
endif()