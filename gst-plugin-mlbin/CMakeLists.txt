cmake_minimum_required(VERSION 3.16)
project(GST_PLUGIN_QTI_OSS_ML_BIN
  VERSION ${GST_PLUGINS_QTI_OSS_VERSION}
  LANGUAGES C
)

set(CMAKE_INCLUDE_CURRENT_DIR ON)

include_directories(${SYSROOT_INCDIR})
link_directories(${SYSROOT_LIBDIR})

find_package(PkgConfig)

# Get the pkgconfigs exported by the automake tools
pkg_check_modules(GST
  REQUIRED gstreamer-1.0>=${GST_VERSION_REQUIRED})
pkg_check_modules(GST_BASE
  REQUIRED gstreamer-base-1.0>=${GST_VERSION_REQUIRED})
pkg_check_modules(GST_ALLOC
  REQUIRED gstreamer-allocators-1.0>=${GST_VERSION_REQUIRED})
pkg_check_modules(GST_QCOM_UTILS
  REQUIRED gstreamer-qcom-oss-utils-1.0>=1.0.0)
pkg_check_modules(GST_QCOM_ML
  REQUIRED gstreamer-qcom-oss-ml-1.0>=1.0.0)

# Generate configuration header file.
configure_file(config.h.in config.h @ONLY)
include_directories(${CMAKE_CURRENT_BINARY_DIR})

# Precompiler definitions.
add_definitions(-DHAVE_CONFIG_H)

# Common compiler flags.
set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -Wall -Wextra -Werror")
set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -Wno-unused-parameter")

# GStreamer ML TFLite Bin plugin.
set(GST_QTI_ML_BIN gstqtimlvideotflitebin)

add_library(${GST_QTI_ML_BIN} SHARED
  mlbin.c
)

target_compile_definitions(${GST_QTI_ML_BIN} PRIVATE
  GST_ML_BIN_STRUCT_NAME=GstMLTFLiteBin
  GST_ML_BIN_STRUCT_CLASS_NAME=GstMLTFLiteBinClass
  GST_ML_BIN_CORE=tflite
  GST_ML_BIN_TYPE=video
)

target_include_directories(${GST_QTI_ML_BIN} PUBLIC
  ${GST_INCLUDE_DIRS}
  ${GST_QCOM_ML_INCLUDE_DIRS}
  ${GST_QCOM_UTILS_INCLUDE_DIRS}
)

target_link_libraries(${GST_QTI_ML_BIN} PRIVATE
  ${GST_LIBRARIES}
  ${GST_BASE_LIBRARIES}
  ${GST_ALLOC_LIBRARIES}
  ${GST_QCOM_ML_LIBRARIES}
  ${GST_QCOM_UTILS_LIBRARIES}
)

install(
  TARGETS ${GST_QTI_ML_BIN}
  LIBRARY DESTINATION ${GST_PLUGINS_QTI_OSS_INSTALL_LIBDIR}/gstreamer-1.0
  PERMISSIONS OWNER_EXECUTE OWNER_WRITE OWNER_READ
              GROUP_EXECUTE GROUP_READ
              WORLD_EXECUTE WORLD_READ
)

# GStreamer ML QNN Bin plugin.
set(GST_QTI_ML_BIN gstqtimlvideoqnnbin)

add_library(${GST_QTI_ML_BIN} SHARED
  mlbin.c
)

target_compile_definitions(${GST_QTI_ML_BIN} PRIVATE
  GST_ML_BIN_STRUCT_NAME=GstMLQnnBin
  GST_ML_BIN_STRUCT_CLASS_NAME=GstMLQnnBinClass
  GST_ML_BIN_CORE=qnn
  GST_ML_BIN_TYPE=video
)

target_include_directories(${GST_QTI_ML_BIN} PUBLIC
  ${GST_INCLUDE_DIRS}
  ${GST_QCOM_ML_INCLUDE_DIRS}
  ${GST_QCOM_UTILS_INCLUDE_DIRS}
)

target_link_libraries(${GST_QTI_ML_BIN} PRIVATE
  ${GST_LIBRARIES}
  ${GST_BASE_LIBRARIES}
  ${GST_ALLOC_LIBRARIES}
  ${GST_QCOM_ML_LIBRARIES}
  ${GST_QCOM_UTILS_LIBRARIES}
)

install(
  TARGETS ${GST_QTI_ML_BIN}
  LIBRARY DESTINATION ${GST_PLUGINS_QTI_OSS_INSTALL_LIBDIR}/gstreamer-1.0
  PERMISSIONS OWNER_EXECUTE OWNER_WRITE OWNER_READ
              GROUP_EXECUTE GROUP_READ
              WORLD_EXECUTE WORLD_READ
)

# GStreamer ML SNPE Bin plugin.
set(GST_QTI_ML_BIN gstqtimlvideosnpebin)

add_library(${GST_QTI_ML_BIN} SHARED
  mlbin.c
)

target_compile_definitions(${GST_QTI_ML_BIN} PRIVATE
  GST_ML_BIN_STRUCT_NAME=GstMLSnpeBin
  GST_ML_BIN_STRUCT_CLASS_NAME=GstMLSnpeBinClass
  GST_ML_BIN_CORE=snpe
  GST_ML_BIN_TYPE=video
)

target_include_directories(${GST_QTI_ML_BIN} PUBLIC
  ${GST_INCLUDE_DIRS}
  ${GST_QCOM_ML_INCLUDE_DIRS}
  ${GST_QCOM_UTILS_INCLUDE_DIRS}
)

target_link_libraries(${GST_QTI_ML_BIN} PRIVATE
  ${GST_LIBRARIES}
  ${GST_BASE_LIBRARIES}
  ${GST_ALLOC_LIBRARIES}
  ${GST_QCOM_ML_LIBRARIES}
  ${GST_QCOM_UTILS_LIBRARIES}
)

install(
  TARGETS ${GST_QTI_ML_BIN}
  LIBRARY DESTINATION ${GST_PLUGINS_QTI_OSS_INSTALL_LIBDIR}/gstreamer-1.0
  PERMISSIONS OWNER_EXECUTE OWNER_WRITE OWNER_READ
              GROUP_EXECUTE GROUP_READ
              WORLD_EXECUTE WORLD_READ
)

# GStreamer ML ONNX Bin plugin.
set(GST_QTI_ML_BIN gstqtimlvideoonnxbin)

add_library(${GST_QTI_ML_BIN} SHARED
  mlbin.c
)

target_compile_definitions(${GST_QTI_ML_BIN} PRIVATE
  GST_ML_BIN_STRUCT_NAME=GstMLOnnxBin
  GST_ML_BIN_STRUCT_CLASS_NAME=GstMLOnnxBinClass
  GST_ML_BIN_CORE=onnx
  GST_ML_BIN_TYPE=video
)

target_include_directories(${GST_QTI_ML_BIN} PUBLIC
  ${GST_INCLUDE_DIRS}
  ${GST_QCOM_ML_INCLUDE_DIRS}
  ${GST_QCOM_UTILS_INCLUDE_DIRS}
)

target_link_libraries(${GST_QTI_ML_BIN} PRIVATE
  ${GST_LIBRARIES}
  ${GST_BASE_LIBRARIES}
  ${GST_ALLOC_LIBRARIES}
  ${GST_QCOM_ML_LIBRARIES}
  ${GST_QCOM_UTILS_LIBRARIES}
)

install(
  TARGETS ${GST_QTI_ML_BIN}
  LIBRARY DESTINATION ${GST_PLUGINS_QTI_OSS_INSTALL_LIBDIR}/gstreamer-1.0
  PERMISSIONS OWNER_EXECUTE OWNER_WRITE OWNER_READ
              GROUP_EXECUTE GROUP_READ
              WORLD_EXECUTE WORLD_READ
)
